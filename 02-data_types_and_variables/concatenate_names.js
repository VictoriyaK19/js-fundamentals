function concatenate(strind1, string2, delimiter) {
    console.log(`${strind1}${delimiter}${string2}`);
}
concatenate('John', 'Smith', '->'
);
concatenate('Jan',
'White',
'<->'
);
concatenate('Linda',
'Terry',
'=>'
);
